Internal notes (do not publish):

- Circle demo scripts copied into ./circle-dcw-demo (no .env, no node_modules, no recovery files)
- Use Circle demo .env values locally by exporting env vars in your shell.
- Map variables:
  - ESCROW_WALLET_ID should be a DCW wallet ID (in demo: WALLET_A_ID)
  - Destination is an EVM address (demo: WALLET_B_ADDRESS)
  - USDC token id (demo: USDC_ID)
